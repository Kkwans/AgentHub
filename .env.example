AGENTHUB_HOST=127.0.0.1
AGENTHUB_PORT=3210
AGENTHUB_DATA_DIR=.agenthub
# 取消 Run 等待 Agent 确认的最长时间（毫秒，1000-120000；默认 10000）。超时后状态收敛为 CANCELED，Session 标记为 DISCONNECTED。
AGENTHUB_RUN_CANCEL_TIMEOUT_MS=10000
AGENTHUB_APPROVAL_DELIVERY_TIMEOUT_MS=10000
# 非 loopback 监听时必须配置。首次访问页面创建管理员账号，浏览器使用 HttpOnly Cookie。
# AGENTHUB_AUTH_MODE=token
# 仅用于旧版 API/回滚兼容；普通网页登录不需要。
# AGENTHUB_BOOTSTRAP_TOKEN=请使用高熵随机值并通过环境或 secret manager 注入
# PostgreSQL 模式可选；未设置时使用 PGlite 文件模式。
# DATABASE_URL=postgresql://agenthub:agenthub@127.0.0.1:5432/agenthub
