# AgentHub 管理员登录 TX5Pro 实机验收

- 日期：2026-08-10
- 结果：PASS
- TX5Pro：Windows 10 build 26200、Node.js 24.0.0、Google Chrome 150.0.7871.182
- 地址：`http://192.168.5.110:3210`
- 访问方式：真实 LAN 直连正式 Docker Compose 服务
- 服务状态：尚未认领管理员账号，浏览器进入首次设置页

## 用户流程

首次访问只要求设置用户名、密码和确认密码。页面没有展示 API token、Session、NAS
文件路径或命令行操作；创建完成后由服务端签发 HttpOnly Cookie，后续访问使用普通的
用户名/密码登录。正式管理员凭据由产品所有者自行选择，本次验收没有创建、读取或保存
用户密码。

## 最终检查

- TX5Pro 直接访问 `/api/v1/health`，返回 `v0.3.0/PGlite/Web` 可用；
- 1440 与 390 视口均正确展示“创建管理员账号”；
- 用户名、密码、确认密码和主操作按钮均可由可访问名称定位；
- 两种视口的根页面均无横向溢出；
- 0 request failure、console error、page error、HTTP 4xx/5xx 和外部请求；
- 页面不展示 token、Session 或命令行。

机器可读结果见 [report.json](report.json)，同目录保存桌面与移动端最终截图。第一次脚本运行
发现测试选择器会同时匹配“密码”和“确认密码”；将选择器改为精确匹配后，同一正式服务与
同一 TX5Pro 的最终运行通过。该问题只存在于验收脚本，不是页面运行时错误。

## 验收边界

本次没有替产品所有者认领管理员账号，因此没有在正式库中保存未知密码。登录、Cookie
WebSocket、退出、改密、会话撤销、错误密码和限流由自动化测试覆盖；账号创建后的正式页面
验收可在所有者完成首次设置后继续，且不需要向开发或 QA 提供密码。
