# Real Data Fixtures

必须生成 deterministic fixtures：

- 8 standard project
- 20 test project
- 50 sessions
- 30 closed / 5 failed / 2 approval / 2 running / rest ready
- 200 git entries，nested dirs，long names
- modified/add/delete/rename/untracked/binary
- 100 prompt assets，20 versions，15 bindings
- 1000 tool events，500 messages
- long Chinese + English + emoji + code blocks

fixture 不应写死 `v0.7` 名称。
