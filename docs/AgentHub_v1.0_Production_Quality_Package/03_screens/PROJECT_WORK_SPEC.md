# Project Work v1.0

工作列表优先级：Needs attention → Running → Ready → Backlog → Done。

Row 60–72px；title/description/agent/status/updated。不要把 Goal/Task/Run 所有字段并列。

Task detail 使用 side panel/drawer，避免列表跳整页。Start/Open Session 是行级主要动作。
