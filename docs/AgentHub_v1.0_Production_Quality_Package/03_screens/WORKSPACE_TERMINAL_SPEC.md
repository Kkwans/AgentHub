# Workspace Terminal Spec

Terminal 默认隐藏；从 topbar/keyboard 打开 bottom dock。高度 default 280，min 160 max 55vh，可 resize/persist。

打开后 Conversation 可用高度减少，但 Composer 仍固定可见。多个 terminal future-ready，但 v1.0 可先 single tab。
