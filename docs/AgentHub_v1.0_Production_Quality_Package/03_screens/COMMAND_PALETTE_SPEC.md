# Command Palette Screen Spec

Width 640–680；top search 44；results max 480。

Group heading 24px；result 48px。Result：icon 24 + title/subtitle + shortcut/meta。

输入 `>theme` 搜 command；`AgentHub` 搜 project/session；支持最近项优先。
