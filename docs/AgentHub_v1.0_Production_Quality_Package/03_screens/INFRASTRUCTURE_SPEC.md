# Infrastructure v1.0

Runtime / Remote Node / Diagnostics 是运维型 secondary experience。

采用 compact table/status rows，不用大量 card。核心列：name, kind, connectivity, capability, last seen。

危险/诊断信息通过 detail drawer 展示。
