# Agent Center v1.0

Entity list：Agent / kind-provider / runtime / readiness / model summary / last seen / actions。

Ready/Offline 用 dot+text。Discovery 是 top-right CTA，进入独立 guided flow。

不在 Agent Center 首屏塞 Remote Node diagnostics、Docker details、raw executable args。
