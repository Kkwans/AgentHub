# Workspace Activity Spec

Timeline 聚合 tool calls / commands / file operations / plan updates。

默认 compact row 40–48px；filter by type/status；失败项自动高亮。点击展开 payload/stdout；raw JSON 再次折叠到 Diagnostics。

Conversation 中只保留 summary link 到这里。
