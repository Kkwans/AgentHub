# Project Context v1.0

Header：Project identity / branch / status / primary action。Local tabs：Overview / Work / Sessions。

不要重复全局 page title + project title。进入 Project Context 后 Global page header 退化为 compact identity header。

Overview 使用 2-column：main activity 1fr + project facts 320px；空白侧栏自动 collapse 到单栏。
