# Workspace Inspector v1.0

一级：Changes / Files / Activity / Run。

Header 44–48px；tabs 紧凑。每个 tab 自己拥有 toolbar，不再二级 tabs competing。

Changes = master-detail；Files = tree + preview；Activity = timeline/filter；Run = state/tests/context。
